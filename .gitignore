# Local scratch. Model does not source this folder.
tmp/

# Legacy Drive zip downloads (superseded by OSU_SSI_BRIDGE_DATA_LOCAL)
FLUME_DATA/

# Recorders (empty placeholder; dumps go under plot/out)
output/

# Archived EQ dumps (named copies of trial1 / outDIR)
runs/

# Junction → LOCAL opensees_data (not Shared Drive; archive is 2026-OSU-SSI-Bridge/<day>/)
OSU_SSI_BRIDGE_DATA/

# Local mirror + plots (opensees_data/, plots/, mat_extract/; SyncLabBackup.py)
OSU_SSI_BRIDGE_DATA_LOCAL/

# Legacy plot root (migrated to OSU_SSI_BRIDGE_DATA_LOCAL/plots/)
OSU_SSI_PLOTS/

# Matrix-generated knob overrides (recreate with RunTestMatrix.py --row N)
Overrides.tcl

# Figures, EQ dumps, partition maps, JSON copies next to plotters
plot/out/
plot/__pycache__/
plot/*.json

# Python
__pycache__/
*.py[cod]

# Logs
*.log

# Paper PDFs stay local (BibTeX is tracked)
reference/*.pdf

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
*~
*.swp
